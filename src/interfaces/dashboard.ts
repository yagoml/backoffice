export interface IGadgetInfo {
  title: String
  value: Number | String
  percent: Number
  icon: String
  since: String
}

export interface IMostDesired {
  name: String
  image: String
  score: Number
}
