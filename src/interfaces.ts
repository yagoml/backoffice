export interface ISBMenuItem {
  copy: String
  icon: String
  disabled?: Boolean
}

export interface IGadgetInfo {
  title: String
  value: Number | String
  percent: Number
  icon: String
  since: String
}
